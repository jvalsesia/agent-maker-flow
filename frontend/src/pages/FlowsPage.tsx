/** Flows workspace container. Populated by F07 (Canvas) and F10 (Chat). */
export function FlowsPage() {
  return (
    <section aria-label="Flows workspace">
      <h2>Flows</h2>
      <p>The flow canvas and conversational monitor will live here (F07, F10).</p>
    </section>
  );
}
