/** Agents workspace container. Populated by F04 (Agents Dashboard). */
export function AgentsPage() {
  return (
    <section aria-label="Agents workspace">
      <h2>Agents</h2>
      <p>Agent registry and configuration will live here (F04).</p>
    </section>
  );
}
