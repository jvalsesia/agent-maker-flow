//! Agent Maker Flow backend entrypoint.
//!
//! Stage 1 establishes a compiling binary. The boot sequence (config, telemetry,
//! database/cache connectivity, migrations, router) is wired in later stages.

fn main() {
    println!("agent-maker-flow backend — scaffold");
}
