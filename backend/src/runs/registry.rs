//! In-memory run registry (filled in at Stage 2).

// Stage 2 introduces the real `RunRegistry`. Stage 1 only needs the module to
// exist so `runs::mod` compiles.

/// Placeholder for the in-memory run registry; built out at Stage 2.
pub struct RunRegistry;
