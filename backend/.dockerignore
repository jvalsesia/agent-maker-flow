# Build artifacts (rebuilt inside the image; huge + host-arch specific).
target/

# Local config / secrets — never bake into the image.
.env
.env.*
!.env.example

# VCS / editor noise
.git
.gitignore
*.md
